/**
 * Scanner texture (TILE_TYPE 14)
 * Uses desk texture as base (already generated by generateTextures)
 */

// Scanner uses the desk texture - it's a workstation
export function createScannerTexture(_scene) {
  // No-op: desk textures are generated once in generateTextures().
  // This avoids duplicate texture registration errors that can black out the canvas.
}
